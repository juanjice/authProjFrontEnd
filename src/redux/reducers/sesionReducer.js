export const initialState = {
    entries: [],
    loading: false,
    hasErrors: false,
  }
  
  export default function sessionReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
  }